# java-spring-study 学习内容规范

本仓库不是只用于验证代码能否编译，而是用于**脱离外部教程独立学习**。每个模块都必须让学习者能够从 README、源码注释、可运行示例和测试中完整理解知识点。

## 一、每个知识点必须回答的问题

新增或修改一个知识点时，README 和代码至少要回答：

1. **它是什么**：给出准确概念、术语和所属层次。
2. **它解决什么问题**：说明没有它时会出现什么问题，为什么需要它。
3. **它怎么工作**：按执行顺序说明对象、线程、调用链、数据流或事务边界。
4. **怎么使用**：给出最小可运行代码、启动命令和预期输出。
5. **关键 API 是什么**：说明核心类、方法、参数、返回值和生命周期。
6. **什么时候使用**：列出适用场景和不适用场景。
7. **容易错在哪里**：记录资源释放、线程安全、代理、异常、配置和版本兼容陷阱。
8. **如何验证**：提供直接方法测试、边界测试和必要的集成测试。
9. **如何继续练习**：给出可以独立完成的改造题，并说明观察点。

不能只写“这是一个 XXX 示例”，也不能只列出 API 名称而不解释执行过程。

## 二、源码注释规范

### 1. 类级注释

每个 Demo、Service、Handler、Controller 和重要模型都要说明：

- 学习目标和现实场景；
- 本类在完整调用链中的位置；
- 运行入口和依赖条件；
- 与相邻类的关系；
- 学完后应该观察到什么结果。

### 2. 方法级注释

公共方法和关键私有方法要说明：

- 输入参数和前置条件；
- 执行步骤和每一步的原因；
- 返回值或异常；
- 线程、事务、资源所有权和副作用；
- 调用方应该如何使用。

### 3. 关键语句注释

涉及框架或并发机制的语句按步骤编号注释。例如：

```java
// 1. 先注册 EventLoopGroup：负责在少量线程上调度多个 Channel。
EventLoopGroup worker = new NioEventLoopGroup();
// 2. 指定 Channel 实现：这里使用 NIO TCP Socket。
bootstrap.channel(NioSocketChannel.class);
// 3. 组装 Pipeline：入站先解码，再进入业务 Handler。
bootstrap.handler(new ChannelInitializer<SocketChannel>() { ... });
```

注释不能只重复代码字面意思；必须解释“为什么这样写”。

## 三、README 规范

每个模块 README 至少包含：

1. 模块目标和前置知识；
2. 完整知识点目录和源码映射；
3. 环境、依赖和启动方式；
4. 最小示例的执行步骤和预期结果；
5. 核心概念的原理和执行流程；
6. API/配置/注解的作用与对比；
7. 常见错误和排查方式；
8. 测试分类及每类测试验证的边界；
9. 动手练习和延伸方向；
10. 已知限制（例如需要外部 Kafka/RabbitMQ、只能在真实 Spring 代理下生效）。

README 中列出的每个主题都必须能在源码或测试中找到对应实现；源码中存在的学习主题也必须出现在 README 中。

## 四、测试规范

- 业务逻辑优先直接调用 Service、Domain、Worker 或 Handler 方法，不通过 Controller 代替业务单元测试。
- 每个功能点至少有一个独立 `@Test`。
- 学习型测试使用有意义的 `@DisplayName`，描述场景、输入和预期结果。
- 纯逻辑测试使用 JUnit/Mockito 隔离外部依赖；框架行为用 Spring、EmbeddedChannel、EmbeddedKafka 等集成测试验证。
- 测试必须覆盖成功、失败、边界、异常和资源释放场景。
- 测试名称要让学习者能看懂“这个测试证明了什么”。

## 五、模块完成检查表

提交模块前逐项检查：

- [ ] README 的知识点目录与源码一致；
- [ ] 每个知识点都有原理、最小示例、运行方式和测试；
- [ ] 关键代码有解释“为什么”，不是只有行尾翻译；
- [ ] 异常、并发、事务、资源释放和生命周期已说明；
- [ ] 示例可以独立运行，外部依赖有明确替代方案；
- [ ] 测试方法使用清晰的 `@DisplayName`；
- [ ] `mvn test` 和全项目 `mvn clean verify` 通过；
- [ ] Checkstyle 通过且没有提交编译产物或敏感凭证。
