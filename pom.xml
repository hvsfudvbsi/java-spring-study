<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
      ============================================================
      java-spring-study 父 POM（聚合模块）
      ------------------------------------------------------------
      本仓库采用 Maven 多模块结构：
        - 外层（本目录）仅是一个目录 + 父 POM，不包含业务代码
        - 内层每个 module-xx-* 是一个独立的学习模块，可单独运行

      构建命令（在仓库根目录执行）：
        mvn clean package            # 构建全部模块
        mvn clean package -pl module-xx -am   # 只构建某个模块（含依赖）
        mvn spring-boot:run -pl module-xx     # 运行某个 Spring Boot 模块
      ============================================================
    -->

    <groupId>com.study</groupId>
    <artifactId>java-spring-study</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>java-spring-study</name>
    <description>Java 21 + Spring Boot 3.5 多模块学习仓库</description>

    <modules>
        <module>module-01-java-basics</module>
        <module>module-02-spring-boot-basics</module>
        <module>module-03-spring-mvc</module>
        <module>module-04-spring-data-jpa</module>
        <module>module-05-spring-security</module>
        <module>module-06-spring-aop</module>
        <module>module-07-spring-boot-advanced</module>
        <module>module-08-spring-cloud</module>
        <module>module-09-message-queue</module>
    </modules>

    <properties>
        <!-- 编译版本：Java 21 -->
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 框架版本：集中管理，子模块无需重复指定 -->
        <spring-boot.version>3.5.16</spring-boot.version>
        <spring-cloud.version>2025.0.3</spring-cloud.version>
        <jjwt.version>0.12.6</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot 依赖清单（管理所有 spring-boot-starter-* 版本） -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud 依赖清单（管理 openfeign / loadbalancer / circuitbreaker 等版本） -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- JWT 库（module-05-spring-security 使用） -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- 编译器插件：支持 Java 21 语法（record、sealed、switch 模式匹配等） -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>

                <!-- 测试插件：支持 JUnit 5 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                </plugin>

                <!-- Spring Boot 打包插件：只有需要打成可执行 jar 的模块才启用 -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
